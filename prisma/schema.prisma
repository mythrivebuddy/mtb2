generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider          = "postgresql"
  url               = env("DATABASE_URL")
  directUrl         = env("DIRECT_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
}

// updated user for item table , updating the model for product purcahsing using razorpay
model User {
  id                            String                            @id @default(uuid())
  email                         String                            @unique
  password                      String?
  name                          String
  createdAt                     DateTime                          @default(now())
  updatedAt                     DateTime                          @updatedAt
  role                          Role
  image                         String?
  jpEarned                      Int                               @default(0)
  jpSpent                       Int                               @default(0)
  authMethod                    AuthMethod                        @default(CREDENTIALS)
  planEnd                       DateTime?
  planId                        String?
  planStart                     DateTime?
  jpBalance                     Int                               @default(0)
  jpTransaction                 Int                               @default(0)
  isBlocked                     Boolean?                          @default(false)
  emailVerificationToken        String?
  emailVerificationTokenExpires DateTime?
  isEmailVerified               Boolean                           @default(false)
  achievements                  String?
  goals                         String?
  keyOfferings                  String?
  referralCode                  String?                           @unique
  referredById                  String?
  socialHandles                 Json?
  website                       String?
  membership                    String                            @default("FREE")
  subscriptionEnd               DateTime?
  subscriptionId                String?                           @unique
  subscriptionStart             DateTime?
  subscriptionStatus            String?
  bio                           String?
  isOnline                      Boolean?                          @default(false)
  isFirstTimeSurvey             Boolean?                          @default(true)
  lastSurveyTime                DateTime?
  challenge_limit               Int                               @default(1)
  currentCurrency               SubscriptionPlanCurrency?
  currentPlanId                 String?
  currentPlanInterval           PlanInterval?
  userType                      PlanUserType?
  ActivityFeedItem              ActivityFeedItem[]
  alignedActions                AlignedAction[]
  billingInformation            BillingInformation?
  blockedUsers                  BlockedUsers?
  buddyLensRequests             BuddyLensRequest[]                @relation("BuddyLensRequests")
  BuddyLensReviewer             BuddyLensRequest[]                @relation("BuddyLensReviewer")
  BuddyLensReview               BuddyLensReview[]
  cart                          Cart[]
  challengeCreatorSignature     ChallengeCreatorSignature?
  challengeMessageReactions     ChallengeMessageReaction[]
  accountabilityComments        Comment[]                         @relation("AccountabilityComments")
  couponRedemptions             CouponRedemption[]
  groupsOwned                   Group[]                           @relation("CoachGroups")
  Group_Group_creatorIdToUser   Group[]                           @relation("Group_creatorIdToUser")
  accountabilityMemberships     GroupMember[]                     @relation("AccountabilityMemberships")
  invoice                       Invoice[]
  magicBoxes                    MagicBox[]
  buddyChecker                  MakeoverAccountabilityBuddy[]     @relation("MakeoverBuddyTarget")
  buddyDoer                     MakeoverAccountabilityBuddy[]     @relation("MakeoverBuddyOwner")
  makeoverProgressLogs          MakeoverProgressLog[]
  mandates                      Mandate[]
  miracleLog                    MiracleLog[]
  Notification                  Notification[]
  oneTimePurchases              OneTimeProgramPurchase[]
  orders                        Order[]
  paymentOrders                 PaymentOrder[]
  profileViews                  ProfileView[]
  progressVault                 ProgressVault[]
  prosperityDrops               ProsperityDrop[]
  pushSubscriptions             PushSubscription[]
  userBilling                UserBillingInformation?
  referralsReceived             Referral[]                        @relation("Referred")
  referralsMade                 Referral[]                        @relation("Referrer")
  reminders                     Reminder[]
  rewardsGiven                  Reward[]                          @relation("AdminRewards")
  rewardsReceived               Reward[]                          @relation("MemberRewards")
  spotlight                     Spotlight[]
  streak                        Streak[]
  streakHistory                 StreakHistory[]
  subscriptions                 Subscription[]
  invoices                      SubscriptionInvoice[]             @relation("UserInvoices")
  sundayProgressLogs            SundayProgressLog[]
  todos                         Todo[]
  transaction                   Transaction[]
  plan                          Plan?                             @relation(fields: [planId], references: [id])
  referredBy                    User?                             @relation("UserReferrer", fields: [referredById], references: [id])
  referrals                     User[]                            @relation("UserReferrer")
  userBusinessProfile           UserBusinessProfile?
  makeoverAreas                 UserMakeoverArea[]
  makeoverBadges                UserMakeoverBadge[]
  makeoverChallengeEnrollments  UserMakeoverChallengeEnrollment[]
  makeoverCommitments           UserMakeoverCommitment[]
  makeoverLevels                UserMakeoverLevel[]
  selfRewards                   UserMakeoverSelfReward[]
  userNotifications             UserNotification[]                @relation("UserNotifications")
  programStates                 UserProgramState[]
  userStreak                    UserStreak?
  visionImages                  UserVisionImage[]
  wishlists                     Wishlist[]
  certificatesIssued            ChallengeCertificate[]            @relation("CertificatesIssuedByUser")
  certificatesReceived          ChallengeCertificate[]            @relation("CertificatesReceivedByUser")
  enrollments                   ChallengeEnrollment[]
  sentChallengeMessages         ChallengeMessage[]                @relation("UserMessages")
  challengePollVotes            ChallengeMessagePollVote[]
  createdChallenges             Challenge[]                       @relation("CreatedChallenges")
  buddyLensReviews              BuddyLensRequest[]                @relation("BuddyLensReviews")
  Goal                          Goal[]                            @relation("GoalToUser")
  createdItems                  Item[]                            @relation("ItemCreator")
  approvedItems                 Item[]                            @relation("ItemApprover")

  @@index([name])
}

model Transaction {
  id         String   @id @default(uuid())
  userId     String
  activityId String
  createdAt  DateTime @default(now())
  jpAmount   Int
  activity   Activity @relation(fields: [activityId], references: [id])
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model Activity {
  id              String          @id @default(uuid())
  activity        ActivityType    @unique
  jpAmount        Int
  transactionType TransactionType @default(CREDIT)
  Transaction     Transaction[]
}

model Plan {
  id              String   @id @default(uuid())
  name            String   @unique
  jpMultiplier    Float    @default(1.0)
  discountPercent Float    @default(0.0)
  durationDays    Int?
  price           String
  createdAt       DateTime @default(now())
  interval        String?
  isActive        Boolean  @default(true)
  paypalPlanId    String?  @unique
  paypalProductId String?
  updatedAt       DateTime @default(now())
  users           User[]
}

model Spotlight {
  id                  String              @id @default(uuid())
  userId              String
  expiresAt           DateTime?
  appliedAt           DateTime            @default(now())
  status              SpotlightStatus     @default(APPLIED)
  defaultDurationDays Int                 @default(1)
  activatedAt         DateTime?
  seenByAdmin         Boolean             @default(false)
  user                User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  activities          SpotlightActivity[]

  @@index([expiresAt])
}

model Blog {
  id        String   @id @default(cuid())
  title     String
  image     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  content   String
  excerpt   String
  readTime  String
  category  String
}

model UserBusinessProfile {
  id                  String   @id @default(cuid())
  userId              String   @unique
  name                String?
  businessInfo        String?
  missionStatement    String?
  goals               String?
  keyOfferings        String?
  achievements        String?
  email               String?  @unique
  phone               String?
  website             String?
  socialHandles       Json?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
  featuredWorkDesc    String?
  featuredWorkImage   String?
  featuredWorkTitle   String?
  isSpotlightActive   Boolean  @default(false)
  priorityContactLink String?
  profileJpRewarded   Boolean  @default(false)
  isProfileComplete   Boolean  @default(false)
  user                User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([name, businessInfo, missionStatement, goals, achievements, keyOfferings])
}

model BlockedUsers {
  id        String   @id @default(cuid())
  userId    String   @unique
  reason    String
  blockedAt DateTime @default(now())
  blockedBy String?
  user      User     @relation(fields: [userId], references: [id])
}

model ProsperityDrop {
  id          String               @id @default(uuid())
  userId      String
  title       String
  description String
  appliedAt   DateTime             @default(now())
  status      ProsperityDropStatus @default(APPLIED)
  user        User                 @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([status])
}

model Faq {
  id       String @id @default(cuid())
  question String
  answer   String
}

model Question {
  id         String   @id @default(uuid())
  name       String
  categoryId String
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

model SpotlightActivity {
  id          String                @id @default(cuid())
  type        SpotlightActivityType
  spotlightId String
  createdAt   DateTime              @default(now())
  spotlight   Spotlight             @relation(fields: [spotlightId], references: [id], onDelete: Cascade)

  @@index([spotlightId])
  @@index([createdAt])
}

model EmailTemplate {
  id          String   @id @default(cuid())
  templateId  String   @unique
  subject     String
  htmlContent String
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

model MiracleLog {
  id               String    @id @default(uuid())
  content          String
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
  deletedAt        DateTime?
  userId           String
  jpPointsAssigned Boolean   @default(false)
  user             User      @relation(fields: [userId], references: [id])
}

model ProgressVault {
  id               String    @id @default(uuid())
  content          String
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @updatedAt
  deletedAt        DateTime?
  userId           String
  jpPointsAssigned Boolean   @default(false)
  user             User      @relation(fields: [userId], references: [id])
}

// updated Item 
model Item {
  id   String @id @default(cuid())
  name String

  // Pricing
  basePrice     Float
  monthlyPrice  Float?
  yearlyPrice   Float?
  lifetimePrice Float?

  // Media
  imageUrl    String
  downloadUrl String?

  // Category
  categoryId String
  category   Category @relation(fields: [categoryId], references: [id])

  // Ownership (NEW)
  createdByUserId String
  createdByRole   String // ADMIN | COACH
  creator         User   @relation("ItemCreator", fields: [createdByUserId], references: [id])

  // Approval system (NEW)
  isApproved       Boolean   @default(false)
  approvedByUserId String?
  approvedAt       DateTime?
  approver         User?     @relation("ItemApprover", fields: [approvedByUserId], references: [id])

  // Existing relations
  cart       Cart[]
  orderItems OrderItem[]
  wishlists  Wishlist[]

  // Timestamps
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([categoryId])
  @@index([createdByUserId])
  @@index([isApproved])
}

// category  model
model Category {
  id        String   @id @default(cuid())
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  items     Item[]
}

model Order {
  id          String      @id @default(cuid())
  userId      String
  totalAmount Float
  status      String      @default("processing")
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  user        User        @relation(fields: [userId], references: [id])
  items       OrderItem[]

  @@index([userId])
}

model OrderItem {
  id              String   @id @default(cuid())
  orderId         String
  itemId          String
  quantity        Int
  priceAtPurchase Float
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  item            Item     @relation(fields: [itemId], references: [id])
  order           Order    @relation(fields: [orderId], references: [id])

  @@index([orderId])
  @@index([itemId])
}

model Wishlist {
  id        String   @id @default(cuid())
  userId    String
  itemId    String
  createdAt DateTime @default(now())
  item      Item     @relation(fields: [itemId], references: [id])
  user      User     @relation(fields: [userId], references: [id])

  @@unique([userId, itemId])
}

model Cart {
  id            String   @id @default(cuid())
  userId        String
  itemId        String
  quantity      Int      @default(1)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  wasInWishlist Boolean  @default(false)
  item          Item     @relation(fields: [itemId], references: [id])
  user          User     @relation(fields: [userId], references: [id])

  @@unique([userId, itemId])
}

model Referral {
  id         String   @id @default(cuid())
  referrerId String
  referredId String
  createdAt  DateTime @default(now())
  referred   User     @relation("Referred", fields: [referredId], references: [id])
  referrer   User     @relation("Referrer", fields: [referrerId], references: [id])

  @@unique([referrerId, referredId])
}

model MagicBox {
  id             String    @id @default(uuid())
  userId         String
  isOpened       Boolean   @default(false)
  jpAmount       Int?
  openedAt       DateTime?
  createdAt      DateTime  @default(now())
  nextBoxAt      DateTime
  randomUserIds  String[]
  selectedUserId String?
  isRedeemed     Boolean   @default(false)
  redeemedAt     DateTime?
  user           User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([nextBoxAt])
}

model MagicBoxSettings {
  id          String   @id @default(uuid())
  minJpAmount Int      @default(100)
  maxJpAmount Int      @default(500)
  updatedAt   DateTime @updatedAt
}

model BuddyLensRequest {
  id             String                 @id @default(uuid())
  requesterId    String
  socialMediaUrl String
  jpCost         Int
  status         BuddyLensRequestStatus @default(OPEN)
  createdAt      DateTime               @default(now())
  completedAt    DateTime?
  tier           String                 @default("5min")
  domain         String
  isDeleted      Boolean                @default(false)
  updatedAt      DateTime               @updatedAt
  questions      String[]
  reviewerId     String?
  requester      User                   @relation("BuddyLensRequests", fields: [requesterId], references: [id])
  reviewer       User?                  @relation("BuddyLensReviewer", fields: [reviewerId], references: [id])
  review         BuddyLensReview[]
  User           User[]                 @relation("BuddyLensReviews")

  @@index([requesterId])
  @@index([status])
}

model BuddyLensReview {
  id          String                @id @default(uuid())
  requestId   String
  rating      Int?
  feedback    String?
  submittedAt DateTime?
  createdAt   DateTime              @default(now())
  updatedAt   DateTime              @updatedAt
  reviewText  String
  reviewerId  String
  answers     String[]
  status      BuddyLensReviewStatus @default(PENDING)
  request     BuddyLensRequest      @relation(fields: [requestId], references: [id])
  reviewer    User                  @relation(fields: [reviewerId], references: [id])

  @@unique([requestId, reviewerId])
  @@index([requestId])
  @@index([reviewerId])
  @@index([status])
}

model UserNotification {
  id        String   @id @default(uuid())
  userId    String
  message   String
  read      Boolean  @default(false)
  createdAt DateTime @default(now())
  link      String?
  user      User     @relation("UserNotifications", fields: [userId], references: [id])
}

model Invoice {
  id                 String   @id @default(uuid())
  userSubscriptionId String
  amount             Float
  saleId             String   @unique
  createdAt          DateTime @default(now())
  user               User     @relation(fields: [userSubscriptionId], references: [subscriptionId])
}

model ProfileView {
  id       String   @id @default(uuid())
  userId   String
  viewedAt DateTime @default(now())
  viewerId String?
  user     User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([viewedAt])
}

model Notification {
  id        String           @id @default(uuid())
  userId    String
  type      NotificationType
  title     String
  message   String
  isRead    Boolean          @default(false)
  createdAt DateTime         @default(now())
  metadata  Json?
  user      User             @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([createdAt])
}

model PushSubscription {
  id        String   @id @default(uuid())
  userId    String
  endpoint  String   @unique
  p256dh    String
  auth      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model AlignedAction {
  id           String   @id @default(uuid())
  userId       String
  mood         String
  completed    Boolean  @default(false)
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  category     String
  reminderSent Boolean  @default(false)
  selectedTask String
  tasks        String[]
  timeFrom     DateTime
  timeTo       DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, createdAt])
  @@index([timeFrom])
}

model Streak {
  id                     String     @id @default(cuid())
  userId                 String
  type                   StreakType
  miracle_log_count      Int        @default(0)
  progress_vault_count   Int        @default(0)
  miracle_log_last_at    DateTime?
  progress_vault_last_at DateTime?
  createdAt              DateTime   @default(now())
  updatedAt              DateTime   @updatedAt
  user                   User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, type])
}

model StreakHistory {
  id        String     @id @default(cuid())
  userId    String
  type      StreakType
  count     Int
  date      DateTime   @default(now())
  createdAt DateTime   @default(now())
  user      User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, type])
}

model UserStreak {
  id            String   @id @default(uuid())
  userId        String   @unique
  currentStreak Int      @default(0)
  lastActiveDay DateTime
  loginCount    Int      @default(0)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  user          User     @relation(fields: [userId], references: [id])
}

model Todo {
  id             String     @id @default(cuid())
  title          String
  description    String?
  dueDate        DateTime?
  frequency      Frequency?
  isCompleted    Boolean    @default(false)
  createdAt      DateTime   @default(now())
  updatedAt      DateTime   @updatedAt
  userId         String
  taskAddJP      Boolean    @default(false)
  taskCompleteJP Boolean    @default(false)
  isFromEvent    Boolean    @default(false)
  user           User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model Challenge {
  id                   String                            @id @default(cuid())
  slug                 String
  title                String
  description          String?
  mode                 ChallengeMode                     @default(PUBLIC)
  cost                 Float                             @default(0)
  reward               Float                             @default(0)
  penalty              Float                             @default(0)
  startDate            DateTime
  endDate              DateTime
  status               ChallengeStatus                   @default(UPCOMING)
  createdAt            DateTime                          @default(now())
  creatorId            String
  social_link_task     String?
  isIssuingCertificate Boolean                           @default(false)
  joinMode             ChallengeJoinMode                 @default(MANUAL)
  makeoverAreaMaps     MakeoverAreaChallengeMap[]
  makeoverEnrollments  UserMakeoverChallengeEnrollment[]
  certificates         ChallengeCertificate[]
  enrollments          ChallengeEnrollment[]
  messages             ChallengeMessage[]                @relation("ChallengeMessages")
  templateTasks        ChallengeTask[]
  creator              User                              @relation("CreatedChallenges", fields: [creatorId], references: [id], onDelete: Cascade)

  @@map("challenges")
}

model ChallengeTask {
  id                String              @id @default(cuid())
  description       String
  challengeId       String
  challenge         Challenge           @relation(fields: [challengeId], references: [id], onDelete: Cascade)
  userTaskInstances UserChallengeTask[]

  @@map("challenge_tasks")
}

model UserChallengeTask {
  id              String              @id @default(cuid())
  description     String
  isCompleted     Boolean             @default(false)
  enrollmentId    String
  templateTaskId  String
  lastCompletedAt DateTime?           @map("last_completed_at")
  enrollment      ChallengeEnrollment @relation(fields: [enrollmentId], references: [id], onDelete: Cascade)
  templateTask    ChallengeTask       @relation(fields: [templateTaskId], references: [id], onDelete: Cascade)

  @@map("user_challenge_tasks")
}

model ChallengeEnrollment {
  id                  String                            @id @default(cuid())
  joinedAt            DateTime                          @default(now())
  status              EnrollmentStatus                  @default(IN_PROGRESS)
  userId              String
  challengeId         String
  currentStreak       Int                               @default(0)
  longestStreak       Int                               @default(0)
  lastStreakUpdate    DateTime?                         @map("last_streak_update")
  isCertificateIssued Boolean                           @default(false)
  makeoverLinks       UserMakeoverChallengeEnrollment[]
  challenge           Challenge                         @relation(fields: [challengeId], references: [id], onDelete: Cascade)
  user                User                              @relation(fields: [userId], references: [id], onDelete: Cascade)
  userTasks           UserChallengeTask[]

  @@unique([userId, challengeId])
  @@map("challenge_enrollments")
}

model ChallengeMessage {
  id          String                     @id @default(cuid())
  message     String?
  createdAt   DateTime                   @default(now())
  userId      String?
  challengeId String
  replyToId   String?
  type        MessageType                @default(USER)
  meta        Json?
  reactions   ChallengeMessageReaction[]
  challenge   Challenge                  @relation("ChallengeMessages", fields: [challengeId], references: [id], onDelete: Cascade)
  replyTo     ChallengeMessage?          @relation("Replies", fields: [replyToId], references: [id])
  replies     ChallengeMessage[]         @relation("Replies")
  user        User?                      @relation("UserMessages", fields: [userId], references: [id], onDelete: Cascade)
  poll        ChallengeMessagePoll?

  @@index([challengeId, createdAt])
  @@map("challenge_messages")
}

model ChallengeMessagePoll {
  id            String                       @id @default(cuid())
  question      String
  createdAt     DateTime                     @default(now())
  allowMultiple Boolean                      @default(true)
  messageId     String                       @unique
  options       ChallengeMessagePollOption[]
  message       ChallengeMessage             @relation(fields: [messageId], references: [id], onDelete: Cascade)

  @@map("challenge_polls")
}

model ChallengeMessagePollOption {
  id     String                     @id @default(cuid())
  text   String
  pollId String
  poll   ChallengeMessagePoll       @relation(fields: [pollId], references: [id], onDelete: Cascade)
  votes  ChallengeMessagePollVote[]

  @@map("challenge_poll_options")
}

model ChallengeMessagePollVote {
  id        String                     @id @default(cuid())
  createdAt DateTime                   @default(now())
  userId    String
  optionId  String
  option    ChallengeMessagePollOption @relation(fields: [optionId], references: [id], onDelete: Cascade)
  user      User                       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, optionId])
  @@map("challenge_poll_votes")
}

model ChallengeMessageReaction {
  id        String           @id @default(cuid())
  emoji     String
  createdAt DateTime         @default(now())
  userId    String
  messageId String
  message   ChallengeMessage @relation(fields: [messageId], references: [id], onDelete: Cascade)
  user      User             @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, messageId])
  @@index([messageId])
}

model ChallengeCertificate {
  id               String    @id @default(cuid())
  certificateId    String    @unique
  participantId    String
  challengeId      String
  issuedById       String
  issuedAt         DateTime  @default(now())
  certificateUrl   String
  qrCodeUrl        String?
  verificationHash String
  imageUrl         String?
  challenge        Challenge @relation(fields: [challengeId], references: [id], onDelete: Cascade)
  issuedBy         User      @relation("CertificatesIssuedByUser", fields: [issuedById], references: [id], onDelete: Cascade)
  participant      User      @relation("CertificatesReceivedByUser", fields: [participantId], references: [id], onDelete: Cascade)

  @@map("challenge_certificates")
}

model ChallengeCreatorSignature {
  id        String        @id @default(cuid())
  userId    String        @unique
  type      SignatureType
  text      String?
  imageUrl  String?
  createdAt DateTime      @default(now())
  user      User          @relation(fields: [userId], references: [id])
}

model NotificationSettings {
  id                String           @id @default(uuid())
  notification_type NotificationType @unique
  title             String
  message           String
  url               String?
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
}

model CompletionRecord {
  id          String   @id @default(cuid())
  date        DateTime @db.Date
  status      String
  userId      String
  challengeId String
  createdAt   DateTime @default(now())

  @@unique([userId, challengeId, date])
  @@map("completion_records")
}

model Reminder {
  id             String    @id @default(cuid())
  title          String
  description    String?
  frequency      Int
  startTime      String?
  endTime        String?
  lastNotifiedAt DateTime?
  isActive       Boolean   @default(true)
  createdAt      DateTime  @default(now())
  updatedAt      DateTime  @updatedAt
  userId         String
  image          String
  user           User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}

model Announcement {
  id              String    @id @default(uuid())
  title           String
  backgroundColor String    @map("background_color")
  fontColor       String    @map("font_color")
  linkUrl         String?   @map("link_url")
  openInNewTab    Boolean?  @default(false) @map("open_in_new_tab")
  isActive        Boolean   @default(true) @map("is_active")
  audience        Audience
  expireAt        DateTime? @map("expire_at")
  createdAt       DateTime  @default(now()) @map("created_at")
  updatedAt       DateTime  @updatedAt @map("updated_at")
}

model Event {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  title        String
  start        DateTime
  end          DateTime?
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  userId       String    @db.Uuid
  description  String?
  is_bloom     Boolean   @default(false)
  is_completed Boolean   @default(false)
  all_day      Boolean   @default(false)

  @@map("Event")
}

model Group {
  id                         String             @id @default(cuid())
  name                       String
  description                String?
  createdAt                  DateTime           @default(now())
  updatedAt                  DateTime           @updatedAt
  visibility                 Visibility         @default(PUBLIC)
  creatorId                  String
  coachId                    String?
  progressStage              ProgressStage      @default(NOT_STARTED)
  notesPrivacy               NotesPrivacy       @default(VISIBLE_TO_GROUP)
  cycleDuration              CycleDuration      @default(MONTHLY)
  notes                      String?
  isBlocked                  Boolean            @default(false)
  activityFeed               ActivityFeedItem[]
  cycles                     Cycle[]
  coach                      User?              @relation("CoachGroups", fields: [coachId], references: [id])
  User_Group_creatorIdToUser User               @relation("Group_creatorIdToUser", fields: [creatorId], references: [id])
  members                    GroupMember[]
  Goal                       Goal[]             @relation("GoalToGroup")
}

model GroupMember {
  userId     String
  groupId    String
  assignedAt DateTime  @default(now())
  assignedBy String
  role       GroupRole @default(MEMBER)
  goals      Goal[]
  group      Group     @relation(fields: [groupId], references: [id], onDelete: Cascade)
  user       User      @relation("AccountabilityMemberships", fields: [userId], references: [id], onDelete: Cascade)

  @@id([userId, groupId])
}

model Cycle {
  id        String   @id @default(cuid())
  startDate DateTime
  endDate   DateTime
  status    String   @default("active")
  createdAt DateTime @default(now())
  updatedAt DateTime
  groupId   String
  group     Group    @relation(fields: [groupId], references: [id], onDelete: Cascade)
  goals     Goal[]
  Reward    Reward[]

  @@index([groupId])
}

model Goal {
  id               String             @id @default(cuid())
  text             String
  description      String?
  isCompleted      Boolean            @default(false)
  createdAt        DateTime           @default(now())
  updatedAt        DateTime           @updatedAt
  midwayUpdate     String?
  endResult        String?
  status           String             @default("IN_PROGRESS")
  stage            ProgressStage      @default(NOT_STARTED)
  authorId         String
  groupId          String
  cycleId          String
  notes            String?
  ActivityFeedItem ActivityFeedItem[]
  comments         Comment[]
  member           GroupMember        @relation(fields: [authorId, groupId], references: [userId, groupId], onDelete: Cascade)
  cycle            Cycle              @relation(fields: [cycleId], references: [id], onDelete: Cascade)
  Group            Group[]            @relation("GoalToGroup")
  User             User[]             @relation("GoalToUser")

  @@unique([authorId, cycleId])
  @@index([authorId])
}

model Comment {
  id               String             @id @default(cuid())
  content          String
  createdAt        DateTime           @default(now())
  updatedAt        DateTime           @updatedAt
  authorId         String
  goalId           String
  ActivityFeedItem ActivityFeedItem[]
  author           User               @relation("AccountabilityComments", fields: [authorId], references: [id], onDelete: Cascade)
  goal             Goal               @relation(fields: [goalId], references: [id], onDelete: Cascade)

  @@index([authorId])
  @@index([goalId])
}

model ActivityFeedItem {
  id        String           @id @default(cuid())
  type      ActivityFeedType
  createdAt DateTime         @default(now())
  actorId   String
  groupId   String
  goalId    String?
  commentId String?
  details   Json?
  User      User             @relation(fields: [actorId], references: [id], onDelete: Cascade)
  Comment   Comment?         @relation(fields: [commentId], references: [id])
  Goal      Goal?            @relation(fields: [goalId], references: [id])
  group     Group            @relation(fields: [groupId], references: [id], onDelete: Cascade)

  @@index([actorId])
  @@index([groupId, createdAt])
}

model Reward {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  jpAmount  Int
  notes     String?
  adminId   String
  memberId  String
  cycleId   String
  admin     User     @relation("AdminRewards", fields: [adminId], references: [id])
  cycle     Cycle    @relation(fields: [cycleId], references: [id])
  member    User     @relation("MemberRewards", fields: [memberId], references: [id])

  @@index([adminId])
  @@index([memberId])
  @@index([cycleId])
}

model Nudge {
  id                   String   @id @default(cuid())
  createdAt            DateTime @default(now())
  updatedAt            DateTime @updatedAt
  groupId              String
  adminId              String
  memberId             String
  pushNotificationSent Boolean  @default(false)
  sendEmail            Boolean  @default(false)
  title                String?
  description          String?
  subject              String?
  emailContent         String?
  url                  String
}

model Page {
  id              String         @id @default(cuid())
  title           String
  slug            String         @unique
  content         Json
  metaTitle       String?
  metaDescription String?
  metaKeywords    String?
  canonicalUrl    String?
  ogTitle         String?
  ogDescription   String?
  ogImage         String?
  schemaType      PageSchemaType @default(NONE)
  schemaMarkup    Json?
  authorId        String?
  isPublished     Boolean        @default(false)
  createdAt       DateTime       @default(now())
  updatedAt       DateTime       @updatedAt
}

model SubscriptionPlan {
  id               String                   @id @default(cuid())
  name             String                   @unique
  userType         PlanUserType
  interval         PlanInterval
  amountINR        Float
  amountUSD        Float
  description      String?
  isActive         Boolean                  @default(true)
  createdAt        DateTime                 @default(now())
  updatedAt        DateTime                 @updatedAt
  gstEnabled       Boolean                  @default(true)
  gstPercentage    Float                    @default(18)
  features         Json?
  isProgramPlan    Boolean                  @default(false)
  programId        String?
  mandates         Mandate[]
  oneTimePurchases OneTimeProgramPurchase[]
  paymentOrders    PaymentOrder[]
  subscriptions    Subscription[]
  invoices         SubscriptionInvoice[]
  program          Program?                 @relation("ProgramPlans", fields: [programId], references: [id])
  coupons          Coupon[]                 @relation("PlanCoupons")
}

model Mandate {
  id              String           @id @default(cuid())
  mandateId       String           @unique
  userId          String
  planId          String
  status          MandateStatus
  currency        String
  paymentMethod   String?
  frequency       PlanInterval
  maxAmount       Float
  nextBillingDate DateTime?
  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
  plan            SubscriptionPlan @relation(fields: [planId], references: [id])
  user            User             @relation(fields: [userId], references: [id])
  subscriptions   Subscription?
}

model Subscription {
  id                  String                  @id @default(cuid())
  userId              String
  planId              String
  mandateId           String?                 @unique
  couponId            String?
  status              SubscriptionStatus
  startDate           DateTime
  endDate             DateTime
  renewedAt           DateTime?
  trialEndsAt         DateTime?
  createdAt           DateTime                @default(now())
  updatedAt           DateTime                @updatedAt
  orderId             String?
  grantedByPurchaseId String?                 @unique
  paymentOrderId      String?
  coupon              Coupon?                 @relation(fields: [couponId], references: [id])
  grantedByPurchase   OneTimeProgramPurchase? @relation("FreeSubscriptionGrant", fields: [grantedByPurchaseId], references: [id])
  mandate             Mandate?                @relation(fields: [mandateId], references: [id])
  plan                SubscriptionPlan        @relation(fields: [planId], references: [id])
  user                User                    @relation(fields: [userId], references: [id])
  invoices            SubscriptionInvoice[]
}

model PaymentOrder {
  id              String           @id @default(cuid())
  userId          String
  planId          String
  orderId         String?          @unique
  currency        String
  status          PaymentStatus
  paymentMethod   String?
  paymentId       String?
  couponId        String?
  billingInfoId   String?
  createdAt       DateTime         @default(now())
  paidAt          DateTime?
  cashfreeOrderId String?
  baseAmount      Float
  discountApplied Float            @default(0)
  gstAmount       Float            @default(0)
  totalAmount     Float
  coupon          Coupon?          @relation(fields: [couponId], references: [id])
  plan            SubscriptionPlan @relation(fields: [planId], references: [id])
  user            User             @relation(fields: [userId], references: [id])
}

model SubscriptionInvoice {
  id                   String           @id @default(cuid())
  subscriptionId       String
  userId               String
  planId               String
  baseAmount           Float
  gstAmount            Float            @default(0)
  totalAmount          Float
  currency             String
  serialNumberIN       Int?
  serialNumberINTL     Int?
  transactionId        String?
  status               InvoiceStatus
  billingDate          DateTime
  createdAt            DateTime         @default(now())
  billingInformationId String?
  plan                 SubscriptionPlan @relation(fields: [planId], references: [id])
  subscription         Subscription     @relation(fields: [subscriptionId], references: [id])
  user                 User             @relation("UserInvoices", fields: [userId], references: [id])
}

model Coupon {
  id                   String                     @id @default(cuid())
  couponCode           String                     @unique
  description          String?
  type                 CouponType
  discountPercentage   Float?
  freeDays             Int?
  applicableUserTypes  CouponUserType[]
  applicableCurrencies SubscriptionPlanCurrency[]
  firstCycleOnly       Boolean                    @default(false)
  multiCycle           Boolean                    @default(false)
  startDate            DateTime
  endDate              DateTime
  maxGlobalUses        Int?
  maxUsesPerUser       Int?                       @default(1)
  autoApply            Boolean                    @default(false)
  autoApplyConditions  Json?
  status               CouponStatus
  createdBy            String?
  createdAt            DateTime                   @default(now())
  updatedAt            DateTime                   @updatedAt
  discountAmountINR    Float?
  discountAmountUSD    Float?
  redemptions          CouponRedemption[]
  oneTimePurchases     OneTimeProgramPurchase[]
  paymentOrders        PaymentOrder[]
  subscriptions        Subscription[]
  applicablePlans      SubscriptionPlan[]         @relation("PlanCoupons")
}

model CouponRedemption {
  id              String   @id @default(cuid())
  couponId        String
  userId          String
  usedAt          DateTime @default(now())
  appliedPlan     String
  discountApplied Float
  coupon          Coupon   @relation(fields: [couponId], references: [id])
  user            User     @relation(fields: [userId], references: [id])
}

model BillingInformation {
  id           String   @id @default(cuid())
  userId       String   @unique
  fullName     String
  email        String
  phone        String?
  addressLine1 String
  addressLine2 String?
  city         String
  state        String
  postalCode   String
  country      String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  user         User     @relation(fields: [userId], references: [id])
}

// userbilling for products

model UserBillingInformation {
  id           String   @id
  userId       String   @unique
  phone        String?
  addressLine1 String
  addressLine2 String?
  city         String
  state        String
  postalCode   String
  country      String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  user         User     @relation(fields: [userId], references: [id])
}

model AdminCashfreeConfigSettings {
  id           Int      @id @default(autoincrement())
  cashfreeMode String   @default("sandbox")
  updatedAt    DateTime @updatedAt
}

model Program {
  id                             String                   @id @default(cuid())
  name                           String                   @unique
  slug                           String                   @unique
  description                    String?
  durationDays                   Int?
  isOneTimeProduct               Boolean                  @default(true)
  isActive                       Boolean                  @default(true)
  grantsFreeSubscriptionToPlanId String?
  createdAt                      DateTime                 @default(now())
  updatedAt                      DateTime                 @updatedAt
  endDate                        DateTime?
  startDate                      DateTime?
  onboardingEndDate              DateTime?
  onboardingStartDate            DateTime?
  max_golbal_points              Int?
  purchases                      OneTimeProgramPurchase[]
  plans                          SubscriptionPlan[]       @relation("ProgramPlans")
  userProgramStates              UserProgramState[]
}

model OneTimeProgramPurchase {
  id                 String           @id @default(cuid())
  productId          String
  planId             String
  userId             String
  orderId            String?
  cashfreeOrderId    String?
  baseAmount         Float
  gstAmount          Float            @default(0)
  discountApplied    Float            @default(0)
  totalAmount        Float
  currency           String
  couponId           String?
  status             PaymentStatus
  purchasedAt        DateTime         @default(now())
  freeSubscriptionId String?          @unique
  coupon             Coupon?          @relation(fields: [couponId], references: [id])
  plan               SubscriptionPlan @relation(fields: [planId], references: [id])
  product            Program          @relation(fields: [productId], references: [id])
  user               User             @relation(fields: [userId], references: [id])
  freeSubscription   Subscription?    @relation("FreeSubscriptionGrant")
}

model MakeoverArea {
  id            Int                        @id
  name          String                     @unique
  description   String
  challengeMaps MakeoverAreaChallengeMap[]
  progressLogs  MakeoverProgressLog[]
  userAreas     UserMakeoverArea[]
  commitments   UserMakeoverCommitment[]
}

model MakeoverAreaChallengeMap {
  id          String       @id @default(cuid())
  programId   String
  areaId      Int
  challengeId String
  area        MakeoverArea @relation(fields: [areaId], references: [id])
  challenge   Challenge    @relation(fields: [challengeId], references: [id], onDelete: Cascade)

  @@unique([programId, areaId])
}

model UserMakeoverArea {
  id        String       @id @default(cuid())
  userId    String
  programId String
  areaId    Int
  year      Int
  area      MakeoverArea @relation(fields: [areaId], references: [id])
  user      User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId, areaId])
}

model MakeoverGoalLibrary {
  id       String  @id @default(cuid())
  areaId   Int
  title    String
  isCustom Boolean @default(false)

  @@unique([areaId, title])
}

model MakeoverIdentityLibrary {
  id        String  @id @default(cuid())
  areaId    Int
  statement String
  isCustom  Boolean @default(false)

  @@unique([areaId, statement])
}

model MakeoverDailyActionLibrary {
  id       String  @id @default(cuid())
  areaId   Int
  title    String
  isCustom Boolean @default(false)

  @@unique([areaId, title])
}

model UserMakeoverCommitment {
  id              String        @id @default(cuid())
  userId          String
  programId       String
  areaId          Int?
  quarter         String
  goalId          String?
  goalText        String?
  identityId      String?
  identityText    String?
  actionId        String?
  actionText      String?
  visionStatement String?
  isLocked        Boolean       @default(false)
  area            MakeoverArea? @relation(fields: [areaId], references: [id])
  user            User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId, areaId, quarter])
}

model UserVisionImage {
  id        String   @id @default(cuid())
  userId    String
  programId String
  quarter   String
  imageUrl  String
  isLocked  Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId, quarter])
}

// usdf kdfjksdbnfkljbksldnf
model UserProgramState {
  id                       String    @id @default(cuid())
  userId                   String
  programId                String
  onboarded                Boolean   @default(false)
  onboardedAt              DateTime?
  createdAt                DateTime  @default(now())
  updatedAt                DateTime  @updatedAt
  lastReminderDate         DateTime?
  inactivity3DayNotified   DateTime?
  inactivity7DayNotified   DateTime?
  lastGoaMilestoneNotified Int       @default(0)
  program                  Program   @relation(fields: [programId], references: [id])
  user                     User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId])
}

model MakeoverProgressLog {
  id            String       @id @default(cuid())
  userId        String
  programId     String
  areaId        Int
  date          DateTime     @db.Date
  identityDone  Boolean?
  actionDone    Boolean?
  winLogged     Boolean?
  weeklyWin     String?
  identityShift String?
  nextWeekFocus String?
  sharedInGroup Boolean      @default(false)
  pointsEarned  Int          @default(0)
  completedAt   DateTime     @default(now())
  area          MakeoverArea @relation(fields: [areaId], references: [id])
  user          User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId, areaId, date])
}

// sunday progressing 
model SundayProgressLog {
  id                  String   @id @default(cuid())
  userId              String
  programId           String
  date                DateTime
  card1WeeklyWin      Boolean  @default(false)
  card1DailyWin       Boolean  @default(false)
  card1NextWeekDone   Boolean  @default(false)
  card2Done           Boolean  @default(false)
  card3Done           Boolean  @default(false)
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt
  sundayCardId        Int
  card1NextWeekTaskId String?
  user                User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId, sundayCardId, date], name: "uniq_sunday_progress")
  @@index([userId])
  @@index([programId])
}

model MakeoverCompletionSnapshot {
  id                   String @id @default(cuid())
  userId               String
  programId            String
  areaId               Int
  month                String
  completionPercentage Float

  @@unique([userId, programId, areaId, month])
}

model MakeoverPointsSummary {
  id          String   @id @default(cuid())
  userId      String
  programId   String
  areaId      Int
  totalPoints Int      @default(0)
  lastUpdated DateTime @updatedAt

  @@unique([userId, programId, areaId])
}

model MakeoverLevel {
  id            Int                 @id
  name          String
  minPoints     Int
  icon          String?
  identityState String?
  levelTheme    String?
  users         UserMakeoverLevel[]
}

model UserMakeoverLevel {
  id        String        @id @default(cuid())
  userId    String
  programId String
  levelId   Int
  earnedAt  DateTime      @default(now())
  level     MakeoverLevel @relation(fields: [levelId], references: [id])
  user      User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId])
}

model MakeoverBadge {
  id              String              @id @default(cuid())
  name            String              @unique
  thresholdPoints Int?
  icon            String?
  type            MakeoverBadgeType?
  description     String?
  users           UserMakeoverBadge[]
}

model UserMakeoverBadge {
  id        String        @id @default(cuid())
  userId    String
  programId String
  badgeId   String
  earnedAt  DateTime      @default(now())
  badge     MakeoverBadge @relation(fields: [badgeId], references: [id])
  user      User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId, badgeId])
}

model MakeoverAccountabilityBuddy {
  id          String @id @default(cuid())
  userId      String
  buddyUserId String
  programId   String
  areaId      Int
  quarter     String
  buddyUser   User   @relation("MakeoverBuddyTarget", fields: [buddyUserId], references: [id], onDelete: Cascade)
  user        User   @relation("MakeoverBuddyOwner", fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId, areaId, quarter])
}

model UserMakeoverChallengeEnrollment {
  id           String              @id @default(cuid())
  userId       String
  programId    String
  areaId       Int
  challengeId  String
  enrollmentId String
  challenge    Challenge           @relation(fields: [challengeId], references: [id], onDelete: Cascade)
  enrollment   ChallengeEnrollment @relation(fields: [enrollmentId], references: [id], onDelete: Cascade)
  user         User                @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId, areaId])
}

model MakeoverSelfRewardLibrary {
  id          String                         @id @default(cuid())
  title       String
  description String?
  levelId     Int
  levelName   String
  minPoints   Int
  isActive    Boolean                        @default(true)
  createdAt   DateTime                       @default(now())
  updatedAt   DateTime                       @updatedAt
  checkpoints MakeoverSelfRewardCheckpoint[]
  options     MakeoverSelfRewardOption[]
}

model MakeoverSelfRewardOption {
  id          String                    @id @default(cuid())
  libraryId   String
  title       String
  description String?
  isActive    Boolean                   @default(true)
  createdAt   DateTime                  @default(now())
  library     MakeoverSelfRewardLibrary @relation(fields: [libraryId], references: [id], onDelete: Cascade)
  userRewards UserMakeoverSelfReward[]
}

model MakeoverSelfRewardCheckpoint {
  id              String                    @id @default(cuid())
  levelId         Int
  minPoints       Int
  orderIndex      Int
  createdAt       DateTime                  @default(now())
  rewardLibraryId String
  rewardLibrary   MakeoverSelfRewardLibrary @relation(fields: [rewardLibraryId], references: [id])
  userRewards     UserMakeoverSelfReward[]

  @@unique([levelId, minPoints])
  @@index([levelId])
}

model UserMakeoverSelfReward {
  id                      String                       @id @default(cuid())
  userId                  String
  programId               String
  levelId                 Int
  levelName               String
  checkpointId            String
  isCustom                Boolean                      @default(false)
  customTitle             String?
  customDescription       String?
  pointsAtUnlock          Int
  unlockedAt              DateTime                     @default(now())
  completedAt             DateTime?
  rewardOptionId          String?
  unclaimedReminderSentAt DateTime?
  checkpoint              MakeoverSelfRewardCheckpoint @relation(fields: [checkpointId], references: [id], onDelete: Cascade)
  rewardOption            MakeoverSelfRewardOption?    @relation(fields: [rewardOptionId], references: [id])
  user                    User                         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, programId, checkpointId])
  @@index([userId, programId, levelId])
}

model AdminPaymentGatewayConfig {
  id            Int      @id @default(autoincrement())
  activeGateway String   @default("CASHFREE")
  updatedAt     DateTime
}

enum ChallengeJoinMode {
  MANUAL
  SYSTEM_ONLY
}

enum MessageType {
  USER
  SYSTEM
}

enum SubscriptionPlanCurrency {
  INR
  USD
}

enum PlanUserType {
  COACH
  ENTHUSIAST
  SOLOPRENEUR
  ALL
}

enum PlanInterval {
  MONTHLY
  YEARLY
  LIFETIME
  FREE
  ONE_TIME
}

enum MandateStatus {
  PENDING
  ACTIVE
  FAILED
  CANCELLED
}

enum SubscriptionStatus {
  ACTIVE
  CANCELLED
  EXPIRED
  FREE_TRIAL
  PENDING
  CANCELLATION_PENDING
  FREE_GRANT
  CREATED
  AUTHORIZED
  DROPPED
}

enum InvoiceStatus {
  PAID
  FAILED
}

enum CouponType {
  PERCENTAGE
  FIXED
  FREE_DURATION
  FULL_DISCOUNT
  AUTO_APPLY
}

enum CouponUserType {
  COACH
  ENTHUSIAST
  ALL
  SOLOPRENEUR
}

enum CouponStatus {
  ACTIVE
  INACTIVE
  EXPIRED
}

enum PaymentStatus {
  PAID
  PENDING
  FAILED
  CREATED
  AUTHORIZED
  DROPPED
  CANCELLED
  REFUNDED
  PARTIALLY_REFUNDED
}

enum MakeoverBadgeType {
  LEVEL
  MILESTONE
}

enum PageSchemaType {
  NONE
  ARTICLE
  FAQ_PAGE
  HOW_TO
  CUSTOM_JSON
}

enum Audience {
  EVERYONE
  PAID
  FREE
}

enum Role {
  ADMIN
  USER
}

enum AuthMethod {
  GOOGLE
  CREDENTIALS
}

enum ActivityType {
  SIGNUP
  DAILY_LOGIN
  QUIZ_CORRECT
  ADD_LOG
  SPOTLIGHT
  PROSPERITY_DROP
  BUSINESSPROFILE_COMPLETE
  MIRACLE_LOG
  PROGRESS_VAULT
  REFER_BY
  REFER_TO
  MAGIC_BOX_REWARD
  MAGIC_BOX_SHARED_REWARD
  GENERAL_FEEDBACK
  FEATURE_REQUEST
  BUG_REPORT
  ALIGNED_ACTION
  STREAK_7_DAYS
  STREAK_21_DAYS
  STREAK_45_DAYS
  STREAK_90_DAYS
  MIRACLE_STREAK_REWARD_7_DAYS
  MIRACLE_STREAK_REWARD_21_DAYS
  MIRACLE_STREAK_REWARD_45_DAYS
  MIRACLE_STREAK_REWARD_90_DAYS
  PROGRESS_VAULT_STREAK_REWARD_7_DAYS
  PROGRESS_VAULT_STREAK_REWARD_21_DAYS
  PROGRESS_VAULT_STREAK_REWARD_45_DAYS
  PROGRESS_VAULT_STREAK_REWARD_90_DAYS
  BUDDY_LENS_REQUEST
  BUDDY_LENS_REVIEW
  DAILY_BLOOM_CREATION_REWARD
  DAILY_BLOOM_COMPLETION_REWARD
  CHALLENGE_CREATION_FEE
  CHALLENGE_JOINING_FEE
  CHALLENGE_FEE_EARNED
  CHALLENGE_PENALTY
  CHALLENGE_REWARD
  REWARD_JP_TO_MEMBER_BY_COACH
  COACH_REWARD_SPEND
  COACH_REWARD_RECEIVE
}

enum TransactionType {
  DEBIT
  CREDIT
}

enum SpotlightStatus {
  APPLIED
  IN_REVIEW
  APPROVED
  DISAPPROVED
  EXPIRED
  ACTIVE
}

enum ProsperityDropStatus {
  APPLIED
  IN_REVIEW
  APPROVED
  DISAPPROVED
}

enum SpotlightActivityType {
  VIEW
  CONNECT
}

enum BuddyLensRequestStatus {
  OPEN
  CLAIMED
  COMPLETED
  CANCELLED
  PENDING
}

enum BuddyLensReviewStatus {
  DRAFT
  SUBMITTED
  REPORTED
  PENDING
  APPROVED
  DISAPPROVED
}

enum NotificationType {
  JP_EARNED
  PROSPERITY_APPLIED
  SPOTLIGHT_APPROVED
  SPOTLIGHT_ACTIVE
  MAGIC_BOX_SHARED
  SPOTLIGHT_APPLIED
  PROSPERITY_APPROVED
  BUDDY_LENS_CLAIMED
  BUDDY_LENS_APPROVED
  BUDDY_LENS_REJECTED
  BUDDY_LENS_REVIEWED
  BUDDY_LENS_COMPLETED
  JP_SPEND
  DAILY_BLOOM_REMINDER
  DAILY_BLOOM_PUSH_NOTIFICATION
  DAILY_CHALLENGE_PUSH_NOTIFICATION
  CMP_DAILY_PRIMARY
  CMP_DAILY_GENTLE_NUDGE
  CMP_SUNDAY_MORNING
  CMP_SUNDAY_EVENING_PENDING
  CMP_QUARTER_ENDING_SOON
  CMP_QUARTER_RESET
  CMP_REWARD_UNLOCKED
  CMP_REWARD_UNCLAIMED
  CMP_LEVEL_UP
  CMP_GOA_PROGRESS_MILESTONE
  CMP_GOA_ELIGIBLE
  CMP_INACTIVITY_3_DAYS
  CMP_INACTIVITY_7_DAYS
  CMP_ONBOARDING_PENDING
}

enum BreathWorkType {
  OM
  VOOO
}

enum StreakType {
  MIRACLE_LOG
  PROGRESS_VAULT
}

enum Frequency {
  Daily
  Weekly
  Monthly
}

enum ChallengeMode {
  PUBLIC
  PERSONAL
}

enum ChallengeStatus {
  UPCOMING
  ACTIVE
  COMPLETED
}

enum EnrollmentStatus {
  IN_PROGRESS
  COMPLETED
  FAILED
}

enum Visibility {
  PUBLIC
  PRIVATE
  MEMBERS_CAN_SEE_GOALS
}

enum CycleDuration {
  MONTHLY
  QUARTERLY
  CUSTOM
}

enum ProgressStage {
  NOT_STARTED
  IN_PROGRESS
  COMPLETED
  BLOCKED
  STAGE_2
  STAGE_3
}

enum NotesPrivacy {
  PRIVATE_TO_AUTHOR
  VISIBLE_TO_GROUP
}

enum ActivityFeedType {
  GOAL_CREATED
  GOAL_COMPLETED
  COMMENT_ADDED
  USER_JOINED_GROUP
  CYCLE_STARTED
  CYCLE_ENDED
  GROUP_CREATED
  GOAL_UPDATED
  STATUS_UPDATED
  GROUP_LEAVE
  GROUP_BLOCKED_BY_ADMIN
}

enum GroupRole {
  ADMIN
  MEMBER
}

enum SignatureType {
  IMAGE
  TEXT
  DRAWN
}
